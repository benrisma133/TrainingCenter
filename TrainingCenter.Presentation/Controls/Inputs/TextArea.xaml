<UserControl x:Class="TrainingCenter.Presentation.Controls.TextArea"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TrainingCenter.Presentation.Helpers"
             x:Name="Root"
             MinWidth="200">

    <UserControl.Resources>
        <!-- Converters -->
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:PlaceholderVisibilityConverter x:Key="PlaceholderVisibilityConverter"/>

        <!-- Smooth animations -->
        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0:0:0" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2" Value="10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <!-- Placeholder Style -->
        <Style x:Key="PlaceholderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9CA3AF"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="12,10,12,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Opacity" Value="0.8"/>
        </Style>

        <!-- Scrollbar Styles -->
        <Style x:Key="ScrollBarThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#CBD5E1" 
                            CornerRadius="4"
                            Width="6">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#94A3B8"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Margin="0,4">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label with Character Counter -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock x:Name="LabelText" 
                       FontSize="13" 
                       FontWeight="Medium"
                       Text="{Binding Label, ElementName=Root}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#374151"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsValid, ElementName=Root}" Value="False">
                                    <Setter Property="Foreground" Value="#EF4444"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock x:Name="AsteriskText" 
                       Text=" *" 
                       Foreground="#EF4444" 
                       FontSize="13" 
                       FontWeight="Medium">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsValid, ElementName=Root}" Value="False">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>

            <!-- Character Counter -->
            <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Visibility="{Binding ShowCharacterCount, ElementName=Root, Converter={StaticResource BoolToVisibilityConverter}}">
                <TextBlock x:Name="CharCountText" 
                       FontSize="12" 
                       FontWeight="Medium">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#6B7280"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsValid, ElementName=Root}" Value="False">
                                    <Setter Property="Foreground" Value="#EF4444"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Grid>

        <!-- TextBox Container -->
        <Border x:Name="TextBoxBorder"
            Grid.Row="1"
            Background="White"
            BorderThickness="1.5"
            CornerRadius="{Binding BorderRadius, ElementName=Root}"
            Height="{Binding AreaHeight, ElementName=Root}"
            Cursor="IBeam"
            RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <TranslateTransform/>
            </Border.RenderTransform>
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#9CA3AF"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetProperty="(Border.BorderBrush).(SolidColorBrush.Color)"
                                                  To="#9CA3AF" Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=InputBox, Path=IsFocused}" Value="True">
                            <Setter Property="BorderBrush" Value="#3B82F6"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsValid, ElementName=Root}" Value="False">
                            <Setter Property="BorderBrush" Value="#EF4444"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <!-- Placeholder -->
                <TextBlock x:Name="PlaceholderText"
                       Style="{StaticResource PlaceholderStyle}"
                       Text="{Binding Placeholder, ElementName=Root}">
                    <TextBlock.Visibility>
                        <MultiBinding Converter="{StaticResource PlaceholderVisibilityConverter}">
                            <Binding ElementName="InputBox" Path="Text"/>
                            <Binding ElementName="InputBox" Path="IsFocused"/>
                        </MultiBinding>
                    </TextBlock.Visibility>
                </TextBlock>

                <!-- Actual TextBox with Custom Scrollbar -->
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="0,0,4,0">
                    <ScrollViewer.Resources>
                        <Style TargetType="ScrollBar" BasedOn="{StaticResource ModernScrollBar}"/>
                    </ScrollViewer.Resources>
                    <TextBox x:Name="InputBox"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalContentAlignment="Top"
                         Padding="12,10"
                         Background="Transparent"
                         BorderThickness="0"
                         FontSize="14"
                         Foreground="#1F2937"
                         CaretBrush="#3B82F6"
                         Text="{Binding Text, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Stretch"
                         GotFocus="InputBox_GotFocus"
                         LostFocus="InputBox_LostFocus">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost" 
                                                        Focusable="False"
                                                        HorizontalScrollBarVisibility="Hidden"
                                                        VerticalScrollBarVisibility="Hidden"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Helper Text (Optional) -->
        <TextBlock x:Name="HelperTextBlock"
               Grid.Row="2"
               Foreground="#6B7280"
               FontSize="12"
               Margin="0,6,0,0"
               TextWrapping="Wrap"
               Text="{Binding HelperText, ElementName=Root}"
               Visibility="{Binding HelperText, ElementName=Root, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Validation Message -->
        <TextBlock x:Name="ValidationMessage"
               Grid.Row="3"
               Foreground="#EF4444"
               FontSize="12"
               Margin="0,6,0,0"
               TextWrapping="Wrap"
               FontWeight="Medium"
               Text="{Binding ValidationMessageText, ElementName=Root}"
               Visibility="{Binding IsValid, ElementName=Root, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
    </Grid>
</UserControl>