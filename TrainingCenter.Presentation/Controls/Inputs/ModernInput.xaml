<UserControl x:Class="TrainingCenter.Presentation.Controls.ModernInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrainingCenter.Presentation.Helpers"
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="100" d:DesignWidth="400">

    <UserControl.Resources>
        <local:PlaceholderVisibilityConverter x:Key="PlaceholderVisibilityConverter"/>

        <Storyboard x:Key="ShakeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="0:0:0"    Value="0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.05" Value="-10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.1"  Value="10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.15" Value="-10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.2"  Value="10"/>
                <EasingDoubleKeyFrame KeyTime="0:0:0.25" Value="0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>

        <Style x:Key="PlaceholderStyle" TargetType="TextBlock">
            <Setter Property="Foreground"       Value="#475569"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin"           Value="12,0,0,0"/>
            <Setter Property="FontSize"         Value="14"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <TextBlock x:Name="LabelText"
                       FontSize="13"
                       FontWeight="Medium">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#94A3B8"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=Root, Path=IsValid}" Value="False">
                                <Setter Property="Foreground" Value="#EF4444"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <TextBlock x:Name="AsteriskText"
                       Text=" *"
                       Foreground="#EF4444"
                       FontSize="13"
                       FontWeight="Medium">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ElementName=Root, Path=IsValid}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>

        <!-- TextBox Container -->
        <Border x:Name="TextBoxBorder"
                Grid.Row="1"
                Background="#252538"
                BorderThickness="1.5"
                Height="44"
                Cursor="IBeam">

            <Border.RenderTransform>
                <TranslateTransform/>
            </Border.RenderTransform>

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="#2D2D44"/>
                    <Setter Property="CornerRadius" Value="8"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#3B4060"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding ElementName=InputBox, Path=IsFocused}" Value="True">
                            <Setter Property="BorderBrush"     Value="#3B82F6"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ElementName=Root, Path=IsValid}" Value="False">
                            <Setter Property="BorderBrush"     Value="#EF4444"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <!-- Placeholder -->
                <TextBlock x:Name="PlaceholderText"
                           Style="{StaticResource PlaceholderStyle}"
                           Text="Enter text...">
                    <TextBlock.Visibility>
                        <MultiBinding Converter="{StaticResource PlaceholderVisibilityConverter}">
                            <Binding ElementName="InputBox" Path="Text"/>
                            <Binding ElementName="InputBox" Path="IsFocused"/>
                        </MultiBinding>
                    </TextBlock.Visibility>
                </TextBlock>

                <!-- TextBox -->
                <TextBox x:Name="InputBox"
                         Background="Transparent"
                         BorderThickness="0"
                         VerticalContentAlignment="Center"
                         Padding="12,0"
                         FontSize="14"
                         Foreground="#F1F5F9"
                         CaretBrush="#3B82F6"
                         TextChanged="InputBox_TextChanged"
                         GotFocus="InputBox_GotFocus"
                         LostFocus="InputBox_LostFocus">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <ScrollViewer x:Name="PART_ContentHost"
                                                      Focusable="False"
                                                      HorizontalScrollBarVisibility="Hidden"
                                                      VerticalScrollBarVisibility="Hidden"/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </Grid>
        </Border>

        <!-- Validation Message -->
        <TextBlock x:Name="ValidationMessage"
                   Grid.Row="2"
                   Foreground="#EF4444"
                   FontSize="12"
                   Margin="0,6,0,0"
                   TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ElementName=Root, Path=IsValid}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</UserControl>