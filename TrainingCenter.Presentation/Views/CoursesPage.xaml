<UserControl x:Class="TrainingCenter.Presentation.Views.CoursesPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cards="clr-namespace:TrainingCenter.Presentation.Controls.Cards"
             xmlns:views="clr-namespace:TrainingCenter.Presentation.Views"
             Background="{StaticResource AppBackgroundBrush}"
             Loaded="CoursesPage_Loaded"
             SizeChanged="CoursesPage_SizeChanged">

    <Grid>

        <!-- LIST VIEW -->
        <Grid x:Name="ListView">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TOP BAR -->
            <Border Grid.Row="0" Padding="24,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <Border Grid.Column="0"
                            Background="{StaticResource InputBackgroundBrush}"
                            BorderBrush="{StaticResource BorderDefaultBrush}"
                            BorderThickness="1" CornerRadius="8"
                            Height="42" MaxWidth="360"
                            HorizontalAlignment="Left">
                        <Grid Margin="12,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Viewbox Width="16" Height="16" Margin="0,0,8,0"
                                     VerticalAlignment="Center">
                                <Canvas Width="24" Height="24">
                                    <Path Data="M21 21L16.514 16.506M19 10.5C19 15.194 15.194 19 10.5 19C5.806 19 2 15.194 2 10.5C2 5.806 5.806 2 10.5 2C15.194 2 19 5.806 19 10.5Z"
                                          Stroke="{StaticResource TextMutedBrush}"
                                          StrokeThickness="2" Fill="Transparent"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"/>
                                </Canvas>
                            </Viewbox>
                            <TextBox x:Name="SearchBox"
                                     Grid.Column="1"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{StaticResource TextPrimaryBrush}"
                                     CaretBrush="{StaticResource AccentBrush}"
                                     FontSize="14" VerticalAlignment="Center"
                                     TextChanged="SearchBox_TextChanged"/>
                            <TextBlock x:Name="SearchPlaceholder"
                                       Grid.Column="1"
                                       Text="Search courses..."
                                       FontSize="14"
                                       Foreground="{StaticResource TextPlaceholderBrush}"
                                       VerticalAlignment="Center"
                                       IsHitTestVisible="False"/>
                        </Grid>
                    </Border>

                    <!-- Add Button -->
                    <Button Grid.Column="1"
                            Style="{StaticResource PrimaryButton}"
                            Width="150"
                            Click="AddCourseBtn_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" FontSize="18" FontWeight="Bold"
                                       Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Add Course" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- CARDS AREA -->
            <ScrollViewer Grid.Row="1"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="16,0,16,16">
                <Grid>
                    <!-- Empty State -->
                    <StackPanel x:Name="EmptyState"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Margin="0,80" Visibility="Collapsed">
                        <TextBlock Text="📚" FontSize="48"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,16"/>
                        <TextBlock Text="No courses found"
                                   FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Add your first course to get started"
                                   FontSize="14"
                                   Foreground="{StaticResource TextMutedBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,8,0,0"/>
                    </StackPanel>

                    <!-- Cards Panel -->
                    <UniformGrid x:Name="CardsPanel"
                                 Columns="3"
                                 Margin="8,0"/>

                    <!-- LOADING OVERLAY -->
                    <Grid x:Name="LoadingOverlay" Visibility="Collapsed">
                        <Border Background="{StaticResource AppBackgroundBrush}" Opacity="0.85"/>
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Grid Width="60" Height="60">
                                <Ellipse Width="50" Height="50"
                                         Stroke="{StaticResource BorderDefaultBrush}"
                                         StrokeThickness="5"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                                <Viewbox Width="50" Height="50">
                                    <Canvas Width="100" Height="100">
                                        <Path Stroke="{StaticResource AccentBrush}"
                                              StrokeThickness="10"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round">
                                            <Path.Data>
                                                <PathGeometry>
                                                    <PathFigure StartPoint="50,10">
                                                        <ArcSegment Point="10,50" Size="40,40"
                                                                    SweepDirection="Clockwise"
                                                                    IsLargeArc="True"/>
                                                    </PathFigure>
                                                </PathGeometry>
                                            </Path.Data>
                                            <Path.RenderTransform>
                                                <RotateTransform x:Name="SpinnerRotate"
                                                                 CenterX="50" CenterY="50"/>
                                            </Path.RenderTransform>
                                            <Path.Triggers>
                                                <EventTrigger RoutedEvent="Path.Loaded">
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation
                                                                Storyboard.TargetName="SpinnerRotate"
                                                                Storyboard.TargetProperty="Angle"
                                                                From="0" To="360"
                                                                Duration="0:0:0.8"
                                                                RepeatBehavior="Forever"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </EventTrigger>
                                            </Path.Triggers>
                                        </Path>
                                    </Canvas>
                                </Viewbox>
                            </Grid>
                            <TextBlock Text="Loading courses..."
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="14" FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Margin="0,16,0,0"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </ScrollViewer>
        </Grid>

        <!-- DETAIL VIEW -->
        <views:CourseDetailView x:Name="DetailView"
                                Visibility="Collapsed"/>
    </Grid>
</UserControl>